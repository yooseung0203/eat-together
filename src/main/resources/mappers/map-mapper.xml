<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Map">
	<insert id="insert">
		insert into map values(map_seq.nextval,#{name},#{address},#{category},#{lat},#{lng},default,#{week_operation},#{time_operation},#{closed},#{phone},#{homepage})
	</insert>
</mapper>