<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Party">
<select resultType="int" id="getNextval">
select party_seq.nextval from dual 
</select>
<insert id="insert">
	insert into party values (#{seq},#{parent_name}, #{parent_address}, #{title}, #{writer}, #{meetdate}, #{count}, #{gender}, #{age}, #{drinking}, #{content},#{status},#{place_id})
</insert>

<select id="selectBySeq" resultType="coma.spring.dto.PartyDTO">
	select * from party where seq = #{value} order by seq desc
</select>

<delete id="delete">
	delete from party where seq = #{value}
</delete>

<update id="update">
      update party set ${parent_name} = #{parent_name}, ${parent_address} = #{parent_address}, ${title} = #{title}, ${meetdate} = #{meetdate}, ${count}=#{count}, ${gender}=#{gender},${age}=#{age},${drinking}=#{drinking},${content}=#{content},${place_id}=#{place_id} where party
 </update>
 
</mapper>