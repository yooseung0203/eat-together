<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Report">
	<insert id="newReport">
		insert into report values(report_seq.nextval,#{category},#{id},#{report_id},sysdate,#{parent_seq})
	</insert>
	<select id="checkDupl" resultType="int">
		select count(*) from report where category=#{category} and id=#{id} and parent_seq=#{parent_seq}
	</select>
</mapper> <!-- by 태훈 모임 글 신고 신고 테이블 추가 -->
   